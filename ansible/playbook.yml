---
- name: Apply common setup to all webservers
  hosts: webservers
  gather_facts: true
  become: true
  roles:
    - common

- name: Configure and deploy proxy services
  hosts: proxy-server
  gather_facts: true
  become: true
  roles:
    - proxy
